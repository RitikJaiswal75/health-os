# India medicine search API (Cloudflare Worker — no trailing slash)
EXPO_PUBLIC_INDIA_CATALOG_URL=https://drugs.healthos.ritik.cc

# Crash-reporting provider config (Cloudflare Worker — no trailing slash)
EXPO_PUBLIC_OBSERVABILITY_CONFIG_URL=https://config.healthos.ritik.cc

# Sentry source-map upload token. Put the real value in .env.local (gitignored), not here.
# SENTRY_AUTH_TOKEN=sntrys_...
